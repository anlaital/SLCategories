#include "Pods-MMMarkdown.xcconfig"
GCC_PREPROCESSOR_DEFINITIONS = COCOAPODS=1
HEADER_SEARCH_PATHS = "${PODS_ROOT}/BuildHeaders" "${PODS_ROOT}/BuildHeaders/MMMarkdown" "${PODS_ROOT}/Headers" "${PODS_ROOT}/Headers/ISO8601DateFormatter" "${PODS_ROOT}/Headers/MMMarkdown"
OTHER_LDFLAGS = -ObjC
PODS_ROOT = ${SRCROOT}